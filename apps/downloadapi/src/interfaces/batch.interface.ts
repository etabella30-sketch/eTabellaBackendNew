export interface serializeBatch {
    batchIndex: number;
    totalSize: number;
    totalParts: number;
}

export interface serializeSmallBatch {
    batchIndex: number;
    totalSize: number;
    totalParts: number;
}